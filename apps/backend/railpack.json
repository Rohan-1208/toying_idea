{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "python": "3.12.8"
  },
  "steps": {
    "install": {
      "commands": [
        "python -m ensurepip --upgrade",
        "python -m pip install --upgrade pip setuptools wheel",
        "sh -c \"python -m pip install -r requirements.txt || python -m pip install -r apps/backend/requirements.txt\""
      ]
    }
  },
  "deploy": {
    "startCommand": "sh -c \"cd apps/backend 2>/dev/null || true; uvicorn app.main:app --host 0.0.0.0 --port $PORT\""
  }
}
